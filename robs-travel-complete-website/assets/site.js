const BUSINESS_PHONE='447771824141';
let pickupPlace=null,destinationPlace=null,lastEstimate=null;

const menu=document.querySelector('.menu'),nav=document.querySelector('.navlinks');
menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));});

function loadMaps(){return new Promise((resolve,reject)=>{if(window.google?.maps?.places)return resolve();const key=String(window.ROBS_TRAVEL_CONFIG?.googleMapsApiKey||'').trim();if(!key)return reject(new Error('Google Maps configuration unavailable'));window.__rtMapsReady=resolve;const s=document.createElement('script');s.src=`https://maps.googleapis.com/maps/api/js?key=${encodeURIComponent(key)}&libraries=places&callback=__rtMapsReady&v=weekly`;s.async=true;s.defer=true;s.onerror=reject;document.head.appendChild(s);});}
function attachPlace(id,setter){const input=document.getElementById(id);const ac=new google.maps.places.Autocomplete(input,{componentRestrictions:{country:'gb'},fields:['formatted_address','geometry','name','place_id']});ac.addListener('place_changed',()=>{const p=ac.getPlace();if(!p.geometry)return;input.value=p.formatted_address||p.name;setter({address:input.value,location:p.geometry.location,placeId:p.place_id});});input.addEventListener('input',()=>setter(null));}
loadMaps().then(()=>{attachPlace('pickup',p=>pickupPlace=p);attachPlace('destination',p=>destinationPlace=p);}).catch(()=>document.getElementById('mapsStatus').textContent='Google address search could not load. Please refresh and try again.');

const runoutBands=[[5,0],[6,10],[7,12],[8,14],[9,16],[10,18],[15,20],[20,30],[25,40],[30,50],[35,60],[40,70],[45,80],[50,90]];
const fixed=[
 {keys:['heathrow'],prices:[400,460,550]},{keys:['gatwick'],prices:[450,500,600]},{keys:['bristol airport','bs48 3dy'],prices:[240,290,340]},{keys:['birmingham airport'],prices:[380,420,490]},{keys:['manchester airport'],prices:[500,570,660]},
 {keys:['exeter airport','ex5 2bd'],prices:[150,170,190]},{keys:['exeter st davids','ex4 4nt'],prices:[120,150,180]},{keys:['newquay airport','tr8 4rq'],prices:[90,110,120]}
];
function vehicleIndex(){const v=document.getElementById('vehicle').value;return v==='standard'?0:v==='mpv'?1:2}
function fixedFare(a,b){const text=(a+' '+b).toLowerCase();const bude=/bude|stratton|poughill|widemouth|marhamchurch|kilkhampton/.test(text);if(!bude)return null;const route=fixed.find(r=>r.keys.some(k=>text.includes(k)));return route?route.prices[vehicleIndex()]:null}
function tariff(date,time){const d=new Date(date+'T'+time);const weekend=[0,6].includes(d.getDay());const hour=Number(time.split(':')[0]);return weekend||hour<7||hour>=19?'night':'day'}
function fareFromMiles(miles,date,time){const type=document.getElementById('vehicle').value;const large=type==='mpv'||type==='minibus';const night=tariff(date,time)==='night';const flag=large?(night?5.8:4.8):(night?4:3.2);const per=large?(night?3.75:3.2):(night?2.75:2.3);const base=Math.max(large?7:5,flag+miles*per)+1;const run=runoutBands.find(x=>miles<=x[0]);return run?Math.ceil(base+run[1]):null}
async function route(){if(!pickupPlace||!destinationPlace)throw new Error('Please choose both addresses from the Google suggestions.');const ds=new google.maps.DirectionsService();return ds.route({origin:{placeId:pickupPlace.placeId},destination:{placeId:destinationPlace.placeId},travelMode:google.maps.TravelMode.DRIVING,unitSystem:google.maps.UnitSystem.IMPERIAL});}

document.getElementById('estimateForm').addEventListener('submit',async e=>{e.preventDefault();const button=document.getElementById('estimateButton'),status=document.getElementById('mapsStatus'),date=document.getElementById('date').value,time=document.getElementById('time').value;if(document.getElementById('vehicle').value==='executive'){status.textContent='Executive journeys are individually quoted. Please call or WhatsApp Rob’s Travel.';return}button.disabled=true;button.textContent='Calculating route…';try{const r=await route(),leg=r.routes[0].legs[0],miles=leg.distance.value/1609.344,minutes=Math.round(leg.duration.value/60);let price=fixedFare(pickupPlace.address,destinationPlace.address)||fareFromMiles(miles,date,time);if(!price)throw new Error('Journeys beyond 50 miles without a fixed fare need a personal quote.');lastEstimate={pickup:pickupPlace.address,destination:destinationPlace.address,date,time,miles,minutes,price,passengers:document.getElementById('passengers').selectedOptions[0].text,vehicle:document.getElementById('vehicle').selectedOptions[0].text};const box=document.getElementById('estimateResult');box.innerHTML=`<span class="eyebrow">YOUR ESTIMATED FARE</span><div class="fare">£${price}</div><div class="route-meta"><span>${miles.toFixed(1)} miles</span><span>about ${minutes} minutes</span><span>${tariff(date,time)} tariff</span></div><p>Guide estimate based on the selected journey. Final booking remains subject to confirmation.</p>`;box.hidden=false;document.getElementById('bookingForm').hidden=false;box.scrollIntoView({behavior:'smooth',block:'center'});status.textContent='Route calculated using Google Maps.';}catch(err){status.textContent=err.message||'The route could not be calculated. Please check the addresses and try again.';}finally{button.disabled=false;button.innerHTML='Calculate instant estimate <span>→</span>';}});

document.getElementById('bookingForm').addEventListener('submit',e=>{e.preventDefault();if(!lastEstimate)return;const name=document.getElementById('customerName').value.trim(),phone=document.getElementById('customerPhone').value.trim(),notes=document.getElementById('notes').value.trim();const lines=["NEW ROB'S TRAVEL BOOKING REQUEST",'',`Estimated fare: £${lastEstimate.price}`,`Pickup: ${lastEstimate.pickup}`,`Destination: ${lastEstimate.destination}`,`Date/time: ${lastEstimate.date} ${lastEstimate.time}`,`Passengers: ${lastEstimate.passengers}`,`Vehicle: ${lastEstimate.vehicle}`,`Route: ${lastEstimate.miles.toFixed(1)} miles / approx. ${lastEstimate.minutes} mins`,'',`Customer: ${name}`,`Phone: ${phone}`,`Notes: ${notes||'None'}`,'','AWAITING CONFIRMATION'];window.open(`https://wa.me/${BUSINESS_PHONE}?text=${encodeURIComponent(lines.join('\n'))}`,'_blank');document.getElementById('bookingForm').hidden=true;document.getElementById('pendingConfirmation').hidden=false;document.getElementById('pendingConfirmation').scrollIntoView({behavior:'smooth',block:'center'});});

const now=new Date(),date=document.getElementById('date'),time=document.getElementById('time');
date.min=now.toISOString().slice(0,10);date.value=date.min;
const rounded=new Date(now.getTime()+4*60*1000);rounded.setMinutes(Math.floor(rounded.getMinutes()/5)*5,0,0);
time.value=String(rounded.getHours()).padStart(2,'0')+':'+String(rounded.getMinutes()).padStart(2,'0');
